# QaaS.Mocker.CommunicationObjects

The QaaS project responsible for handling the shared objects (contracts) of `QaaS.Mocker` and `QaaS.Runner`, and channel
routing states.

> Written In C# 14 & .net 10
